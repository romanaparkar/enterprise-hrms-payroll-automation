// Auth API layer
// TODO (you): create an axios instance pointed at VITE_API_URL,
// then export functions: registerUser(), loginUser().
// Components must never call axios/fetch directly — only through this file.

export {}
