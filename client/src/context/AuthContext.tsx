// AuthContext — global auth state (current user + token)
// TODO (you): create the context, an AuthProvider component that stores
// user/token (persist token in localStorage), and login/logout functions.

export {}
