// Shared auth types
// TODO (you): UserRole ('admin' | 'employee'), JwtPayload (userId, role),
// RegisterInput, LoginInput. Also extend Express's Request with `user`
// (declaration merging) so req.user is typed in middleware/controllers.

export {}
