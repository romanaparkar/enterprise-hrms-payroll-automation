// Auth HTTP layer — thin: parse req, call service, send response
// TODO (you): register(req, res, next) and login(req, res, next).
// Validate required body fields, delegate to auth.service, return JSON.
// Pass errors to next(err) — never try/catch-and-respond here.

export {}
