// Auth business logic — NO req/res in this file
// TODO (you): registerUser(data) → check duplicate email, hash password
// with bcrypt, create user; loginUser(data) → find user (+password),
// compare hash, sign JWT. Both return { user, token }.

export {}
