// Role-based access control middleware
// TODO (you): export requireRole(...roles: UserRole[]) — a middleware
// factory that checks req.user.role and responds 403 if not allowed.
// Must run AFTER auth.middleware.

export {}
