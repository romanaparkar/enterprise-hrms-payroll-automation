// JWT verification middleware
// TODO (you): read "Authorization: Bearer <token>" header, verify with
// jwt.verify(token, env.JWT_SECRET), attach the decoded payload to req.user,
// respond 401 on missing/invalid token.

export {}
