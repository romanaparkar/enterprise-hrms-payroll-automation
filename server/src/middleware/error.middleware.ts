// Centralized error handler — mounted LAST in app.ts
// TODO (you): Express error middleware (err, req, res, next). Map known
// errors (e.g. an ApiError class with statusCode) to proper status codes;
// default to 500. Never leak stack traces in production.

export {}
