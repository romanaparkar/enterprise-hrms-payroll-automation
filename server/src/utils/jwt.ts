// JWT helpers — small pure wrappers around jsonwebtoken
// TODO (you): signToken(payload) and verifyToken(token), typed with
// JwtPayload from types/auth.types.ts. Keeps jwt config in one place.

export {}
