// User model
// TODO (you): Mongoose schema with name, email (unique, lowercase),
// password (select: false), role ('admin' | 'employee', default 'employee'),
// timestamps. Export the model + a TypeScript interface for the document.

export {}
